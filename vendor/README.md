已放入 `足球【官方推荐】.scel`（约 8900 词，偏旧的通名表，几乎不含现役曼城译名）。对照后有用的词再手工并入 YAML，不要整份导入。

把搜狗官网下载的 `.scel` 细胞词库放到这个目录，然后运行：

```
python scripts/parse_scel.py vendor/足球【官方推荐】.scel
```

解析结果只作底库对照，不要直接当最终词库。有用的词再手工并入 `sources/*.yaml`。
